import './UserStories.css'

function StoryWidget({ story }) {
    return (
        <div>
            <img src={story} alt=''></img>
            <p>ari</p>
        </div>
    );
}

export default StoryWidget;
